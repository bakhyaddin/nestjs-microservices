{
  "name": "@sleepr/e2e",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "cross-env NODE_ENV=test jest --preset=ts-jest",
    "test:unit": "cross-env NODE_ENV=test jest --config ./jest-unit.json",
    "test:integration": "cross-env NODE_ENV=test jest --config ./jest-integration.json",
    "test:e2e": "cross-env NODE_ENV=test jest --config ./jest-e2e.json",
    "test:watch": "cross-env NODE_ENV=test jest --watch",
    "test:cov": "cross-env NODE_ENV=test jest --coverage",
    "test:debug": "cross-env NODE_ENV=test node --inspect-brk -r tsconfig-paths/register -r ts-node/register node_modules/.bin/jest --runInBand"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.12.7",
    "@types/tcp-ping": "^0.1.5",
    "cross-env": "^7.0.3",
    "jest": "^29.7.0",
    "tcp-ping": "^0.1.1",
    "ts-jest": "^29.1.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}